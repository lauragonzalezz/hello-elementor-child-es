jQuery(document).ready(function(){
	/*Ocultar secciones con la clase "listed-items" si no tienen ítems en Anywhere Elementor PRO*/
	//jQuery(".listed-items").each(function () {
	//	if (jQuery(this).find(".ae-no-posts").length > 0){ 
	//		jQuery(this).hide();
	//	}
	//});
});